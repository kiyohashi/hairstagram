-# salon,gender,style
= form_with(model: @post, local: true) do |f|
  %h3
    投稿する
  = f.text_field :title
  = f.file_field :image
  = f.text_area :body, placeholder: "text" , rows: "10"
  = f.submit "SEND"